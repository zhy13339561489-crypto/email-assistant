# Email AI 自动整理工具设计方案

## 一、项目概述

开发一个基于 Python 的 Email 自动整理、分类、汇总工具，通过 IMAP 协议连接邮箱，使用 ChatGPT API 对邮件进行智能处理，实现邮件的自动分类、摘要生成和定期汇总。

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      调度层 (Scheduler)                   │
│            APScheduler / 定时触发 / 手动触发               │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                    邮件获取层 (Fetcher)                    │
│              IMAPClient / imaplib 连接邮箱                 │
│              获取未读邮件 / 指定时间范围邮件                  │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                   预处理层 (Preprocessor)                  │
│         解析邮件头 / 提取正文 / 清洗HTML / 附件处理           │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                    AI 处理层 (AI Engine)                   │
│              OpenAI API (GPT-4o / GPT-4o-mini)           │
│              ┌──────────┬──────────┬─────────┐           │
│              │  分类器   │  摘要器   │  提取器  │           │
│              └──────────┴──────────┴─────────┘           │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                    存储层 (Storage)                       │
│              MySQL 数据库存储处理结果                       │
└────────────────────────┬────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────┐
│                   输出层 (Output)                         │
│       控制台报告 / HTML报告 / 邮件转发 / Web Dashboard     │
└─────────────────────────────────────────────────────────┘
```

---

## 三、核心模块设计

### 3.1 配置管理 (`config.py`)

```python
# 使用 YAML 或 .env 管理配置
EMAIL_ACCOUNTS:
  - address: "user@gmail.com"
    imap_server: "imap.gmail.com"
    imap_port: 993
    password: "${EMAIL_PASSWORD}"  # 从环境变量读取
    use_ssl: true

OPENAI_API_KEY: "${OPENAI_API_KEY}"
OPENAI_MODEL: "gpt-4o-mini"  # 成本考虑，默认用 mini

CATEGORIES:
  - 工作
  - 财务
  - 订阅/通知
  - 社交
  - 促销/广告
  - 重要/紧急
  - 垃圾邮件
  - 其他

SCHEDULE:
  interval_minutes: 30
  daily_summary_time: "09:00"
```

### 3.2 邮件获取模块 (`fetcher.py`)

```python
class EmailFetcher:
    """连接邮箱并获取邮件"""

    def connect(self) -> None
        # IMAP 连接，支持 SSL

    def fetch_unread(self) -> list[EmailMessage]
        # 获取未读邮件

    def fetch_by_date(self, since: date, before: date) -> list[EmailMessage]
        # 按日期范围获取

    def mark_as_read(self, uid: str) -> None
        # 标记已读

    def move_to_folder(self, uid: str, folder: str) -> None
        # 移动邮件到指定文件夹（可选）
```

**关键技术点：**
- 使用 `imaplib` 或 `IMAPClient` 库连接邮箱
- 支持多邮箱账户
- 邮件 UID 去重，避免重复处理
- 支持 IDLE 模式实时监听（高级功能）

### 3.3 邮件预处理模块 (`preprocessor.py`)

```python
class EmailPreprocessor:
    """解析和清洗邮件内容"""

    def parse(self, raw_email: bytes) -> EmailData
        # 解析邮件头（发件人、主题、日期）
        # 提取纯文本正文（优先）或 HTML 转文本
        # 处理 MIME 多部分消息
        # 提取附件信息（文件名、类型，不处理内容）

    def clean_text(self, text: str) -> str
        # 去除签名、转发标记等噪音
        # 截断过长邮件（保留前N字符 + 末尾摘要）

    def build_context(self, email: EmailData) -> str
        # 组装发送给 ChatGPT 的上下文文本
```

**输出结构 `EmailData`：**
```python
@dataclass
class EmailData:
    uid: str
    subject: str
    sender: str
    sender_name: str
    to: str
    date: datetime
    body_text: str
    has_attachments: bool
    attachment_names: list[str]
```

### 3.4 AI 处理模块 (`ai_engine.py`)

```python
class AIEngine:
    """调用 ChatGPT 进行邮件分析"""

    def __init__(self, api_key: str, model: str = "gpt-4o-mini"):
        self.client = OpenAI(api_key=api_key)
        self.model = model

    def classify(self, email: EmailData) -> ClassificationResult:
        """
        分类邮件
        返回：分类标签 + 置信度
        """
        # Prompt 设计：
        # - 提供预定义分类列表
        # - 给出发件人、主题、正文摘要
        # - 要求返回 JSON 格式

    def summarize(self, email: EmailData) -> str:
        """
        生成邮件摘要
        返回：2-3句话的中文摘要
        """

    def extract_actions(self, email: EmailData) -> list[ActionItem]:
        """
        提取待办事项/行动项
        返回：需要回复、需要跟进、有截止日期的事项
        """

    def batch_process(self, emails: list[EmailData]) -> list[ProcessResult]:
        """
        批量处理（带速率限制和重试）
        - 使用 asyncio 并发请求
        - 速率限制：每分钟 N 个请求
        - 失败重试：指数退避
        """
```

**Prompt 设计示例（分类）：**

```
你是一个邮件分类助手。请根据以下邮件信息，将其分类到最合适的类别中。

可选分类：{categories}

邮件信息：
- 发件人：{sender}
- 主题：{subject}
- 正文摘要：{body_preview}

请以 JSON 格式返回：
{"category": "分类名", "confidence": 0.95, "reason": "简短理由"}
```

**Prompt 设计示例（摘要）：**

```
请用 2-3 句中文概括以下邮件的核心内容，突出关键信息和行动项。

发件人：{sender}
主题：{subject}
正文：
{body}
```

### 3.5 存储模块 (`storage.py`)

```python
class MySQLEmailStorage:
    """使用 MySQL 存储处理结果"""

    def __init__(self, mysql_config: dict):
        # 建表：emails, categories, summaries, action_items

    def save_result(self, email: EmailData, result: ProcessResult) -> None
    def get_by_category(self, category: str, days: int = 7) -> list
    def get_daily_summary_data(self, date: date) -> dict
    def get_unprocessed_uids(self) -> set[str]
    def get_stats(self, days: int = 30) -> dict
```

**数据库表结构：**

```sql
CREATE TABLE emails (
    id INTEGER PRIMARY KEY,
    uid TEXT UNIQUE,
    account TEXT,
    sender TEXT,
    sender_name TEXT,
    subject TEXT,
    date DATETIME,
    category TEXT,
    confidence REAL,
    summary TEXT,
    has_attachments BOOLEAN,
    processed_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE action_items (
    id INTEGER PRIMARY KEY,
    email_id INTEGER REFERENCES emails(id),
    action TEXT,
    deadline TEXT,
    priority TEXT
);
```

### 3.6 调度与报告模块 (`scheduler.py`, `reporter.py`)

```python
class EmailScheduler:
    """定时任务调度"""

    def start(self):
        # 每 N 分钟检查新邮件
        # 每天固定时间生成日报
        # 每周一生成周报

class Reporter:
    """生成汇总报告"""

    def daily_report(self, date: date) -> str:
        # 按分类统计邮件数量
        # 列出重要邮件摘要
        # 列出待办事项

    def weekly_report(self, start: date, end: date) -> str:
        # 趋势分析
        # 高频发件人
        # 分类分布

    def to_html(self, report: str) -> str
        # HTML 格式化输出
```

---

## 四、项目目录结构

```
email/
├── config.yaml              # 主配置文件
├── .env                     # 环境变量（密码、API Key）
├── requirements.txt         # 依赖清单
├── main.py                  # 入口文件
├── src/
│   ├── __init__.py
│   ├── config.py            # 配置加载
│   ├── fetcher.py           # 邮件获取
│   ├── preprocessor.py      # 邮件预处理
│   ├── ai_engine.py         # ChatGPT 调用
│   ├── storage.py           # 数据存储
│   ├── scheduler.py         # 定时调度
│   └── reporter.py          # 报告生成
├── prompts/                 # Prompt 模板
│   ├── classify.txt
│   ├── summarize.txt
│   └── extract_actions.txt
├── templates/               # HTML 报告模板
│   └── daily_report.html
├── backend/
│   └── storage.py           # MySQL 数据库存储
└── tests/
    ├── test_fetcher.py
    ├── test_preprocessor.py
    └── test_ai_engine.py
```

---

## 五、技术选型

| 模块 | 技术方案 | 说明 |
|------|---------|------|
| 邮件协议 | `imaplib` (标准库) 或 `IMAPClient` | IMAP 读取邮件 |
| 邮件解析 | `email` (标准库) + `beautifulsoup4` | 解析 MIME / HTML 转文本 |
| AI 接口 | `openai` Python SDK | 调用 ChatGPT API |
| 数据库 | `PyMySQL` + MySQL | 统一持久化邮件、日报和箱体 |
| 定时任务 | `APScheduler` | 灵活的调度框架 |
| 配置管理 | `python-dotenv` + `PyYAML` | 环境变量 + YAML 配置 |
| 异步并发 | `asyncio` + `aiohttp` | 批量 API 调用 |
| 日志 | `loguru` | 简洁好用的日志库 |

---

## 六、核心流程

### 6.1 单封邮件处理流程

```
获取未读邮件 → 解析邮件内容 → 截断/清洗正文
       ↓
   发送给 ChatGPT（分类 + 摘要 + 提取待办）
       ↓
   解析 AI 返回结果 → 存入数据库
       ↓
   （可选）标记已读 / 移动到对应文件夹
```

### 6.2 每日汇总流程

```
查询当日处理的所有邮件
       ↓
按分类统计 → 生成统计数据
       ↓
筛选重要邮件 → 生成摘要列表
       ↓
汇总待办事项 → 按优先级排序
       ↓
生成报告（文本 / HTML）→ 输出 / 发送
```

---

## 七、成本控制策略

1. **模型选择**：日常分类用 `gpt-4o-mini`（便宜），重要邮件摘要可用 `gpt-4o`
2. **输入截断**：邮件正文限制 2000 字符，避免长文本消耗 token
3. **批量处理**：合并多封邮件到一次请求（适用于分类场景）
4. **缓存机制**：相同发件人+相似主题的分类结果缓存
5. **速率限制**：控制并发数，避免触发 API 限制

**预估成本（gpt-4o-mini）：**
- 每封邮件处理约消耗 ~500 tokens
- 100 封/天 ≈ $0.01/天 ≈ $0.3/月

---

## 八、安全注意事项

- 邮箱密码和 API Key 仅存于 `.env` 文件，不入版本控制
- `.env` 加入 `.gitignore`
- 邮件内容不落盘（仅存储摘要和元数据）
- 使用 App Password 而非主密码（Gmail 等支持）
- MySQL 账号使用最小权限，密码仅写入 `.env`

---

## 九、扩展方向

| 阶段 | 功能 | 说明 |
|------|------|------|
| v1.0 | 基础功能 | IMAP 获取 → AI 分类/摘要 → 存储 → 控制台报告 |
| v1.1 | 自动回复建议 | AI 生成回复草稿 |
| v1.2 | Web Dashboard | Flask/FastAPI 提供可视化界面 |
| v2.0 | 多邮箱支持 | 同时管理多个邮箱账户 |
| v2.1 | 智能规则学习 | 根据用户反馈自动调整分类规则 |
| v2.2 | 邮件自动归档 | 根据分类自动移动邮件到对应文件夹 |
| v3.0 | 语音/聊天交互 | 通过自然语言查询邮件 |

---

## 十、快速开始

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置环境变量
cp .env.example .env
# 编辑 .env 填入邮箱密码和 OpenAI API Key

# 3. 编辑配置
# 编辑 config.yaml 设置邮箱账户和分类

# 4. 运行
python main.py              # 处理一次新邮件
python main.py --report      # 生成今日报告
python main.py --daemon      # 后台定时运行
```
